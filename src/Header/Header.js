import React from 'react'
import styles from './header.module.css'

export const Header = () => {
  return (
    <div className={`text-center bg-primary text-white ${styles.cl}`}>End to End Application</div>
  )
}
